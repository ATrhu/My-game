#include "physics.h"

float gravityY = 0.0f;

/*bool isOutOfBounds(float x, float y, float radius) {
    return (x - radius < 0) || (x + radius > 1920) || (y - radius < 0) || (y + radius > 1000);
}*/
